<?php
require '../_base.php';

auth('Admin'); 

$stm = $_db->query("SELECT * FROM users");
$user = $stm->fetchAll();

$_title = 'Admin | User Maintenance';
include '../_head.php';
?>

<p><?= count($user) ?> user(s) found.</p>

<table class="table">
    <thead>
        <tr>
            <th>User ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($user as $u): ?>
            <tr>
                <td><?= $u->user_id ?></td>
                <td><?= $u->user_name ?></td>
                <td><?= $u->email ?></td>
                <td>
                    <span class="tag <?= strtolower($u->role) ?>">
                        <?= $u->role ?>
                    </span>
                </td>
                <td>
                    <a href="user_edit.php?id=<?= $u->user_id ?>">Edit</a> | 
                    <button data-post="user_delete.php?id=<?= $u->user_id ?>" data-confirm>Delete</button>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<style>
    .tag { padding: 2px 8px; border-radius: 4px; font-size: 0.8em; }
    .admin { background: #ffcccc; color: #cc0000; font-weight: bold; }
    .member { background: #e0f0ff; color: #0066cc; }
</style>

<?php include '../_foot.php'; 
?>